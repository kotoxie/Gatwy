# Dependencies — installed inside the container by RUN npm install
node_modules
**/node_modules

# Local build outputs — rebuilt inside the container
**/dist

# Git history — not needed in image
.git

# Local data volume
data

# Local moonlight-web binary drops (image downloads release assets)
moonlight-web

# Editor / OS artifacts
.DS_Store
*.log
